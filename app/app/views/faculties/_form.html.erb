<%= form_with(model: faculty, local: true) do |form| %>
  <% if faculty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faculty.errors.count, "error") %> prohibited this faculty from being saved:</h2>

      <ul>
        <% faculty.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :is_part_time %>
    <%= form.check_box :is_part_time %>
  </div>

  <div class="field">
    <%= form.label :departments_id %>
    <%= form.text_field :departments_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
